# Environment variables — NEVER commit these
.env
*.env

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.pyc
venv/
env/
.venv/

# Django
*.log
logs/
staticfiles/
media/
# Local-dev call recordings. VOICE_CALL_RECORDING_ROOT defaults to a
# sibling of MEDIA_ROOT, which lands inside the project on a dev box —
# these are recordings of conversations and must never be committed.
call-recordings/
db.sqlite3

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
# Elastic Beanstalk Files
.elasticbeanstalk/*
!.elasticbeanstalk/*.cfg.yml
!.elasticbeanstalk/*.global.yml
