asgiref==3.11.1
bcrypt==5.0.0
# S3-backed persistent media storage (see backend/settings.py's "AWS S3"
# section and authapp/storage_backends.py) — inert everywhere
# AWS_STORAGE_BUCKET_NAME is unset, which is every environment until it's
# deliberately configured.
boto3==1.35.99
certifi==2026.2.25
cffi==2.0.0
# LIVE-CHAT: Django Channels (real-time WebSocket chat — see
# authapp/consumers/, authapp/routing.py, backend/asgi.py). Only the AWS EB
# deploy runs the ASGI/websocket process (Procfile); cPanel/Passenger keeps
# running WSGI-only and simply doesn't expose /ws/ — unaffected by this.
channels==4.3.2
channels_redis==4.3.0
daphne==4.2.3
redis==8.0.1
charset-normalizer==3.4.6
colorama==0.4.6
cryptography==49.0.0
Django==5.2.12
django-cors-headers==4.9.0
django-storages==1.14.4
djangorestframework==3.17.1
djangorestframework_simplejwt==5.5.1
gunicorn==25.3.0
idna==3.11
packaging==26.0
pillow==12.1.1
pycparser==3.0
PyJWT==2.12.1
PyMySQL==1.1.2
PyOTP==2.10.0
python-decouple==3.8
qrcode==8.2
requests==2.33.0
sqlparse==0.5.5
tzdata==2025.3
urllib3==2.6.3
whitenoise==6.12.0
