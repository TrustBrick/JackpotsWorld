"""
authapp/urls/reward_urls.py
Reward endpoints: list rewards, claim reward, bonus history, bonus claim.
Add your reward views here when ready.
"""
from django.urls import path

# from ..views.reward_views import (
#     RewardListView,
#     ClaimRewardView,
#     BonusHistoryView,
#     BonusClaimView,
# )

urlpatterns = [
    # Uncomment and fill in when reward_views.py is ready:
    # path("rewards/",                RewardListView.as_view()),
    # path("rewards/<int:pk>/claim/", ClaimRewardView.as_view()),
    # path("bonus/history/",          BonusHistoryView.as_view()),
    # path("bonus/claim/",            BonusClaimView.as_view()),
]